services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: chilli
      POSTGRES_DB: main
      POSTGRES_PASSWORD: S3cur3P@ssw0rd!
    ports:
      - 5432:5432
    volumes:
      - ex2_dp_postgres_data:/var/lib/postgresql 
    
volumes:
  ex2_dp_postgres_data:
    external: true